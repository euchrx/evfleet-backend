{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "PRISMA_CLI_BINARY_TARGETS=debian-openssl-3.0.x npm ci && PRISMA_CLI_BINARY_TARGETS=debian-openssl-3.0.x npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "PRISMA_CLI_BINARY_TARGETS=debian-openssl-3.0.x npx prisma migrate deploy && npm run seed && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}